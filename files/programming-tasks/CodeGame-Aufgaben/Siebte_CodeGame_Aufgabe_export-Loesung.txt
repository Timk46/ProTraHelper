        while (not check_destination()):
            if (not check_obstacle_in_direction(ActorDirectionInWorld.NORTH)):
                if (not check_obstacle_in_direction(ActorDirectionInWorld.EAST)):
                    if (not check_obstacle_in_direction(ActorDirectionInWorld.SOUTH)):
                        if (not check_obstacle_in_direction(ActorDirectionInWorld.WEST)):
                            turn(ActorDirection.RIGHT)
                            drive()

            if (check_obstacle_in_direction(ActorDirectionInWorld.SOUTH) or 
                check_obstacle_in_direction(ActorDirectionInWorld.WEST) or
                check_obstacle_in_direction(ActorDirectionInWorld.NORTH) or 
                check_obstacle_in_direction(ActorDirectionInWorld.EAST)):
                drive()