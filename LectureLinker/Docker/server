# Dockerfile
FROM python:3.10

WORKDIR /code

COPY ../app/server /code

RUN pip install --no-cache-dir --upgrade -r /code/requirements.txt

EXPOSE 8000

CMD ["uvicorn", "server:app", "--proxy-headers", "--host", "0.0.0.0", "--port", "3000"]
