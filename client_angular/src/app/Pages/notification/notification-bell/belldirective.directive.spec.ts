import { BelldirectiveDirective } from './belldirective.directive';

describe('BelldirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BelldirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
