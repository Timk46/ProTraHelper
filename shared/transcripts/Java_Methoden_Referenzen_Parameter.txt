Jedes Mal, wenn man ein Objekt hat, zum Beispiel ein Objekt von einem String oder ein Objekt
von einer eigenen Klasse, wie wir bislang gesehen haben, dann wird das nicht kopiert,
dann wird diese Variable, die diesem Objekt eigentlich beinhaltet, eine Referenzvariable
sein, genauso wie bei den Arrays, und da wird dann das nicht passieren.
Da werden wir nicht die Werte kopieren oder da wird das Objekt nicht komplett kopiert,
sodass man dann in diese Funktion mit diesem Objekt irgendwie was machen kann.
Wenn man dann aus dieser Funktion wieder geht, dann hat man eigentlich auf dem eigentlichen
Objekt gearbeitet, nicht auf einer Kopie.
Und man hat nicht die Werten kopiert, man hat eigentlich nur auf den eigentlichen Werten
vom eigentlichen Objekt weitergearbeitet.
Und das ist dann, was man Call-by-Reference nennt.
Und das ist in sehr vielen Programmiersprachen Thema.
Ich erinnere mich noch, dass ich viel C++ gesehen habe, also C++ ist zum Beispiel auch
ein Riesenthema, oder das muss man auch wissen als Programmierer.
Okay, nochmal ein Beispiel, wo das nochmal ganz deutlich wird.
Wir haben hier ein Beispiel, also Klasse hat hier zwei Methoden, swap und m1.
m1 führt swap aus.
Und swap, wenn man das hier so sieht, eigentlich macht es schon Sinn.
Es hat zwei Integers, x und y, und es steckt erstmal den Wert von x in temp, in eine temporäre
Variable, auch Integer, das gilt halt.
Dann nimmt man den Wert von y und steckt das in x.
Und dann nimmt man den Wert, den man dazwischen gespeichert hat, in temp und steckt das in
y.
Das heißt, diese zwei Werten werden getauscht.
Und sobald man hier angekommen ist, ist der Wert, der in x war, jetzt in y und der Wert,
der in y war, war in x.
Deshalb wird diese Funktion auch swap genannt.
Die werden einfach ausgetauscht.
Das Problem aber ist, dass das nicht passiert, weil wir hier mit einfachen Integers arbeiten,
keine Objekte zum Beispiel, und deshalb mit diesen Parametern haben wir Call by Value.
Das heißt, die Werten von a und b hier in der Funktion m1 werden hier so genommen.
Wir haben zwei Variable a und b, beide Integers, die haben einen bestimmten Wert, a hat 1,
b hat 2.
Und wenn wir die hier ausfüllen in die Funktion, dann werden die Werten kopiert und die werden
dann in unsere Swap-Funktion benutzt, aber nur als Werte, nicht als die endgültige Variable
a und b hier.
Das heißt, x bekommt den Wert 1, y bekommt den Wert 2.
Und wenn wir dann zum Beispiel das hier austauschen, das nochmal schön Schritt für Schritt dargestellt,
also Temp bekommt erst den Wert von x, dann bekommt x den Wert von y im nächsten Schritt,
also 2, und dann bekommt y den Wert Temp.
Also wo wir jetzt vorher 1, 2 hatten, haben wir jetzt 2, 1.
Das heißt, man würde denken, dass diese Funktion super funktioniert hat, aber sobald
man dann jetzt aus dieser Funktion geht, wird alles wieder gelöscht und sind a und b noch wie vorher.
Das heißt, diese Swap-Funktion oder Methode hier hat nicht funktioniert, wie wir es gedacht haben.
Der Grund ist, weil a und b Integers sind, also einfache Variablen, wie wir es bislang
immer gesehen und gehandhabt haben, die werden durch Methode gepasst, also by value, also
mit einem Wert.
Das heißt, die Werten werden kopiert und in der Funktion entstehen einfach zwei neue
Integers, die diese Parameter x und y nennen.
Okay, das ist ziemlich wichtig.
Und wie gesagt, das kann man mit anderen Beispielen oder anderen Konstrukten oder anderen Variablen
in diesem Fall anders machen.
Hier machen wir genau das gleiche, nur haben wir jetzt nicht x und y als Integers dargestellt,
aber als Objekte oder als Attribute von einem Objekt von Klasse Pair.
Das heißt, plötzlich haben wir nicht ein Integer, aber ein Objekt von der Klasse und
wie wir gesehen haben, die Objekte von Klassen, die werden in Methoden nicht pass-by-value,
sondern pass-by-reference.
Das sind Referenzvariablen und eigentlich zeigen die auf einen anderen Speicherplatz
und dieses Speicherplatz wird tatsächlich weitergegeben, also diese Referenz wird weitergegeben
an der Funktion.
Das heißt, hier haben wir genau das gleiche für swap, swap ist genau das gleiche, nur
haben wir hier als Parameter ein Objekt P vom Typ oder von Klasse Pair, nicht Typ Klasse.
Und diesem Austausch ist es genauso wie vorher, also genau das gleiche.
Und in M1 passiert auch genau wieder das gleiche, nur haben wir nicht zwei Integers, aber haben
wir halt ein Objekt ab, was wir schon neu kreiert haben mit new und wie wir vorher gesehen
haben mit new, wird dann plötzlich da etwas kreiert im Speicher von Klasse Pair und dann
haben wir gleichzeitig auch sofort diese zwei Attribute x und y, das sind zwei Integers.
Und wenn wir die dann einen bestimmten Wert geben, in diesem Fall können wir das, weil
das ist publik, sonst wäre das hier problematisch gewesen.
Ne, ja doch, wäre es problematisch gewesen.
Ich habe jetzt ein Objekt ab, wo x den Wert 1 hat, y den Wert 2.
Wenn wir jetzt ab weitergeben an diese Funktion, diese Methode swap, dann werden nicht diese
Werte 1 und 2 kopiert und weitergegeben, die Referenz zu diesem Objekt, das hier im Speicher
irgendwo sich befindet, diesem Referenz wird weitergegeben.
Das heißt, diese Referenz zeigt noch immer, die man hier bekommt, zeigt noch immer auf
diesem gleichen Objekt ab hier und die Werten 1 und 2 hier werden auch tatsächlich ausgetauscht.
Um das nochmal anschaulicher zu machen, man hat genau das gleiche wie vorher bei swap.
Das heißt, wir führen hier unsere Methode M1 aus und wir haben in M1 eigentlich nur
eine Variable, nämlich dieses Objekt ab von Klasse Pair.
Das ist aber ein Objekt, deshalb eine Referenz, hat keine Werte hier für sich, ist eine Referenz
auf einen Speicherplatz in unser Heap, auf unser Heap und da hat dieses Objekt zwei Attribute.
x hat den Wert 1, y hat den Wert 2.
Wenn wir das hier jetzt weitergeben, dieses ab an der Methode swap, dann wird einfach
diese Referenz kopiert, aber das heißt, alles was dieses Objekt ab ausmacht, wird dann
eigentlich referiert.
Deshalb werden wir jetzt hier für unsere Methode swap genau das machen.
Wir haben jetzt hier diese Referenz, die aber das gleiche Objekt zeigt und wir werden dann
mit der gleichen Methode, wir stecken erst den Wert von Px in Temp, das haben wir hier
schon gemacht, dann stecken wir in x den Wert von 2, also 2 wird hierhin kopiert und
dann wird von Temp den Wert nach y kopiert.
Und was wir hier sehen ist, dass das hier, was vorher da schon war, würde tatsächlich
ausgetauscht.
Einfach, weil wir hier nichts call by value gemacht haben, aber call by reference, weil
Objekte zum Beispiel immer ein Referenzvariable sind.
Und das ist der Unterschied zwischen beiden.
Das heißt, wenn wir unsere Objekte weitergeben an unsere Funktion, dann können wir die Objekte
auch leicht ändern.
Dann werden die Objekte nicht kopieren und können wir hier in dieser Funktion bliebig
viele Sachen machen.
Die Objekte werden selbst geändert, wie hier dargestellt.