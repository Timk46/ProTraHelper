Sichtbarkeit. Wir haben am Anfang des Kapitels
gesagt, normalerweise sind die
Attributen immer privat.
Das heißt, wenn man ein Objekt kreiert,
irgendwo anders,
zum Beispiel in unserer Main-Methode,
die wir bislang immer gesehen haben,
wir kreieren da ein neues Objekt,
dann kann man via diesem Objekt
normalerweise jede von den Attributen
aufrufen. Man kann die Werten
ändern, man kann die Werten einsehen,
aber das ist eigentlich nicht Teil
des Prinzips von
objektorientierter
Programmierung.
Grundsätzlich möchte man das irgendwie
einkapseln, sodass
man ein klares
Interface, eine klare
Verknüpfung hat zwischen was man programmiert
hat und welche Benutzer
oder was die Benutzer sehen können.
Deshalb hat man normalerweise
alle Attribute
auf private. Also
protected kommt später, das ist auch noch etwas,
das möglich ist, aber
private heißt, dass die Objekte meistens
privat sind. Das heißt, ich kann
nicht einfach nur sagen, O1.a1
in der
vorherigen Folie. Wenn man das macht,
dann hat man einen Kompilierfehler,
wenn man das Ganze auf privat
gestellt hat. Deshalb
macht man das normalerweise mit dieser
Get-und-Set-Methode, die man selbst
kurz
definiert,
wie wir es soeben schon gesehen haben.
Man kann das, oder man macht das
dann typischerweise auch in UML.
Das heißt, plus
ist etwas, das public ist
oder öffentlich ist, minus
ist etwas, das privat ist
und dieses Pound-Symbol
ist für protected.
Das wird eigentlich relevant
gleich nach der Pause, wo wir
dann wirklich Vererbung sehen.
Das heißt, das ist ein Beispiel,
wo wir dann vier verschiedene
Attributen haben.
Ein public oder ein
öffentliches Attribut, zwei private
und ein protected. Alle
integers. Das letzte hier,
hier ist ein Klassenattribut. Das heißt,
das gehört zu der Klasse-Example,
nicht zu den Objekten von
Klasse-Example, die kreiert werden.
Und dann habe auch
das Gleiche, also public, private,
protected oder öffentlich, privat
oder geschützt, kann man auch nehmen
für die Methoden. Das heißt, sowohl
für Attribute als auch für Methoden
kann man kontrollieren, wer diese
Methoden ausführen kann
und wer diese Methode
einsehen kann.
Das heißt,
zum Beispiel,
für unser Beispiel, das wir so
eben hatten, in
Wordle, würde es schon
Sinn machen, zum Beispiel auch das hier
alles als
private zu sehen. Zum Beispiel
the tries, das sehen
andere Benutzer
von unserer Klasse Wordle
nicht eigentlich. Das Gleiche
für pick, auch das ist eigentlich private.
Diese
strings, genau das Gleiche.
Das wird auch private sein und diese
hint eigentlich auch.
Also wenn später, also wenn wir jetzt hier
komplett zum Ende gehen und hier diese
Methode main aufrufen, diese Methode main
kommt aus der Klasse, aber es könnte auch
aus einer anderen Klasse kommen. Und diese
andere Klasse oder in dieser anderen Klasse
können wir zum Beispiel ein neues Objekt
von Klasse Wordle kreieren, wenn wir dann
zum Beispiel sagen, Wordle
.
und dann Word
0 zum Beispiel, dann sollte das ein Fehler geben.
Aber wir kommen nicht mehr an diese Attribute.
Wenn diese main Funktion oder diese main Methode nicht Teil
der Klasse Wordle war, zum Beispiel. Oder das Gleiche mit tries oder alle
andere Sachen auch. Das sind eigentlich alles Attributen, die nur innerhalb der
Klasse wichtig sind, aber für andere Klasse, die diese Klasse zum Beispiel
nutzen würden,
sind die überhaupt nicht relevant. Deshalb brauche ich da auch keine get und set
Methoden. Deshalb habe ich die jetzt einfach nur als privat dargestellt und das war's dann.
Und das Gleiche, wie gesagt, kann man das Gleiche auch machen für die Methoden.
Unsere Konstrukteur, das lassen wir so. Das wird auch benutzt. Aber die Dialog zum Beispiel, auch das
können wir einfach als private umstellen. Das heißt, unser Dialog wird nie ausgeführt,
von einem Objekt, das unsere Klasse benutzt.
Das Gleiche für check, auch das ist eigentlich eine Helper Funktion, die eigentlich nicht relevant ist.
Das Einzige, was publik sein sollte.
Ist unser Run.
Das Einzige, was die Klasse irgendwie erlaubt, ist die Methode auszuführen.
Alles andere sperren wir ab.
Das wäre die Reichtsent 거죠.
Wenn unsere class.
der richtige Weg, um hier zum Beispiel jede Art von
anderer Funktion, auch von anderer Klasse oder insbesondere von
anderer Klasse, das abzuriegeln. Das heißt, diese Klasse ab jetzt
hat eigentlich jetzt alles Privat, das privat sein sollte.
Diese Methode, die wir jetzt soeben auf privat gestellt haben,
die rufen einander auf, aber von dieser Klasse aus,
von unserer Klasse Wordle aus.
Okay. Das nochmal, um dann das deutlich zu machen.
Und sobald wir dann auch Klasse vererben können,
das kommt als nächstes oder im nächsten Kapitel, wird das dann noch
wichtiger werden, weil da werden wir dann zum Beispiel Protected und Private haben
und kann man noch mehr da erzeugen.
Genau, prima. Und dann hier nochmal als
Zusammenfassung für die Get-and-Set-Methoden. Sehr leicht.
Man muss einfach, sobald man erlaubt, dass ein Benutzer
ihre Klasse benutzt, muss man Get-and-Set-Methoden
explizit definieren. Wenn die nicht definiert werden
und alle Attributen auf Private gesetzt werden, kann keine Person
oder kein anderer Entwickler diese Methoden aufrufen
oder diese Attributen ändern oder einsehen.
Und so ist es auch gewollt. Normalerweise sollte man eine Klasse so effizient
und so einfach darstellen.
Alle anderen Variablen und Methoden, die ich da auch implementiert habe,
zum Beispiel so eben für Wordle, das geht eigentlich
anderen Entwicklern nichts an. Die sollen einfach nur mein Objekt von Wordle
erst mal kreieren können, um dann ausführen zu können.
Mit Run. Und das war's. Sonst könnte ich natürlich auch andere Sachen wie
Tries später auch noch veröffentlichen, aber nur wenn es
dann wirklich Sinn macht in der Anwendung.
Genau.
Und hier nochmal ein Beispiel mit mehrer Private Attributen.
Das heißt, die sind alle privat, können nicht irgendwo anders benutzt werden,
können aber in den Methoden von Exempel von Objekte von Exempel schon benutzt werden.
Und wenn man dann die trotzdem irgendwie darstellen möchte für Entwickler,
wenn die zum Beispiel trotzdem ein Objekt von Klasse Exempel ändern können,
das Matrikelnummer zum Beispiel eingeben, dann muss man hier einen Public,
Void Set Matrikelnummer definieren.
In diesem Fall ist das nicht gemacht, weil normalerweise wird ein Matrikelnummer
nur einmal vergeben und wird niemals geändert.
Deshalb ist hier keine Set-Methode definiert und deshalb macht es eigentlich auch Sinn.
Man kann aber die Matrikelnummer auffragen, deshalb mit dieser Get-Methode
kann man fragen, was ist diese Matrikelnummer?
Und das ist ganz anders als zum Beispiel diese Matrikelnummer, die man dann auch
mit der Matrikelnummer definiert.
Und das ist ganz anders als zum Beispiel diese Matrikelnummer, die man dann auch
mit der Matrikelnummer definiert.
Hier, diese Integer-Variablen auf public zu setzen.
Und dann könnte man diese Matrikelnummer natürlich abfragen sofort mit Punkt Matrikelnummer,
und dann könnte man diese Matrikelnummer natürlich abfragen sofort mit Punkt Matrikelnummer,
aber man könnte auch das ändern.
Und das hat man hier so verhindert.
Genau und für Namen und Note kann man genau das gleiche machen.
Gebühr wird hier nicht implementiert, aber genau.
Das ist ungefähr wie das Ganze funktionieren könnte.
Das ist ungefähr wie das Ganze funktionieren könnte.