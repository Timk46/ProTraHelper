Und das allgemeine Schema ist, dass man hier nicht immer nur eine Klassenname hat,
aber dass man auch da ein spezifisches Objekt haben kann,
sodass man auch sieht, dass man zum Beispiel ein Player hat,
das ist ein Objekt von Klasse Snare zum Beispiel.
So das Objekt hier, das ist dann kleingeschrieben
und dann mit diesem Doppelpunkt getrennt von der Klasse.
Und die Klasse wird groß geschrieben.
Und dann hat man, wenn man keine Objekte hat,
wenn man ein Objekt hat, hat man eigentlich kein,
oder müssen sie da auch die Methode nicht darstellen,
weil die gehören sowieso zur Klasse.
Und wenn die Klasse dann angegeben ist,
müssen sie für ein bestimmtes Objekt eigentlich nur die Attributen plus den Wert darstellen.
Das heißt, auch hier hat man eine Liste von Attributen mit vielleicht auch den Werten,
oder sicher den Werten von unserem Objekt.
Das heißt, wir wissen zum Beispiel für ein Beispiel von soeben,
dass unser Player dann eine Länge von 7 hat,
oder war es 8, egal, ein bestimmter Wert.
Oder hier ist noch ein Beispiel, also man hat hier ein Objekt,
das man MyVideo nennt, und das ist ein Objekt,
eine Instanzierung von einem Video.
Video hier groß geschrieben, deshalb Klasse.
Objekt klein geschrieben, hier deshalb ein Objekt.
Und es kann sein, dass die Video-Klasse dann auch noch bestimmte Methoden hat,
das ist aber hier nicht so wichtig.
Das Objekt MyVideo hat bestimmte Daten, die zentral sind,
und die sind schon ausgefüllt.
Das heißt, MyVideo hat einen bestimmten Autor, oder Author,
hat bestimmte Namen zum Beispiel, das ist hier als String dargestellt,
hat eine bestimmte Länge, 3,32 zum Beispiel,
und so weiter, bestimmte Attribute,
die dann irgendwie eine Relevanz für die Anwendung haben.
Also das ist zum Beispiel etwas, was man da machen kann,
oder wie man das Ganze, wie man Objekte darstellen kann.
Und das kann man mischen.
Man kann dann zum Beispiel sagen, ich möchte einfach nur ein Objekt haben,
genauso wie wir vor zwei Folien nur eine Klasse haben.
Und da man es klein geschrieben hat, weiß man,
dass das hier ein Objekt einer Klasse ist.
Und aus dem Kontext sollte dann eigentlich schon,
also wie man das dann verbindet mit anderen Objekten, oder Klassen auch,
sollte dann eigentlich schon deutlich sein, was für eine Klasse das hier war.
Das kann man also weglassen, diese Klasse, Doppelpunktklasse hier.
Oder das Objekt kann man auch weglassen.
Wenn es eigentlich nicht so wichtig ist, welches Objekt man hier hat,
zum Beispiel man möchte einfach nur eine Klasse als Objekt,
um Main auszuführen zum Beispiel, dann kann man genau das hier nehmen,
macht hier einfach ein anonymes Objekt von einer Type Klasse.
Und dann das Allerwichtigste ist,
dass man Objektbeziehungen dann darstellen kann mit Linien.
Meistens sind die horizontal oder vertikal,
das heißt, die gehen hoch oder die gehen von links nach rechts.
Und diese Beziehungen kann man vielfältig darstellen,
sowohl was das bedeutet im Java-Programm,
als auch was das in OML, oder wie man das in OML interpretieren muss.
Und in diesem Teil werde ich kurz und kräftig sagen,
wie man das im OML implementieren muss,
um dann später das etwas deutlicher zu machen.
Das heißt, es gibt hier einen Zwischenhang
zwischen einer bestimmten Klasse, das ist ein Objekt von Klasse 1.
Es gibt einen Zwischenhang mit Objekt 3,
das ist ein spezifisch, ein genanntes Objekt von Klasse 3.
Und dann haben wir hier auch noch ein anonymes Objekt von Klasse 2,
wo wir dann ein Attribut haben, das Attribut hat den Wert rot.
Das ist zum Beispiel, wie die Darstellung dann auch mit Linien
oder mit Verbindungslinien gezeigt werden kann.
Dann muss man auch aufpassen, es kann sein,
dass man zwei Objekte, zum Beispiel wie hier, hat, die genau gleich sind.
Das heißt aber, dass die zwei Objekte trotzdem noch unterschiedlich sind.
Das heißt, man kann eigentlich auch das Ganze als Speicherdiagramm sehen.
Man hat hier im Speicher irgendwo ein Objekt von Klasse Person
und diese Person hat einen Namen und das ist dann typischerweise Mike.
Und es gibt eine Verbindung zwischen diesem Objekt und einem anderen Objekt.
Und dieses Objekt ist auch von Klasse Person,
hat einen anderen Attributwert für Namen, nämlich Dan.
Das heißt, diese Verbindung gibt es,
das ist ein Objekt und das ist ein Objekt irgendwo im Speicher.
Jetzt hat man das Gleiche hier für zwei andere Personen,
diese Person aber nennt sich auch Dan.
Das heißt, das hier und das hier ist zwar gleich,
weil die Attributwerte hier komplett das Gleiche sind,
die sind trotzdem noch in unterschiedlichen Speicherelementen vorhanden.
Das heißt, man könnte später zum Beispiel das hier löschen
und das hier wäre noch immer vorhanden.
Das heißt, die zwei sind gleich, aber die sind nicht identisch.
Und deshalb wird das dann in OML auch dargestellt als zwei Objekte,
die genau gleich aussehen,
aber die auch unterschiedlich einen anderen Platz haben auf dem Papier.
Und deshalb auch gleich sind, aber nicht identisch sind.
Hier rechts ist es identisch.
Hier hat man ein Objekt, dieses Objekt hat auch Namen Dan, oder Dan,
aber wenn man das eine löscht, zum Beispiel via dieser Person,
dann wird das Ganze hier auch gelöscht.
Das heißt, das hier ist nur ein Bereich im Speicher.
Oder wenn man das hier ändert, den Namen ändert,
dann wird dieser Name für beide Verbindungen hier geändert.
Hier nichts.
Das ist dann wirklich der Unterschied zwischen Gleichheit und Identität,
was nicht so wichtig ist.
Das Allerwichtigste ist aber, dass jedes Mal, wenn man so ein Kästchen hat,
dass es auch spezifisch für einen Bereich im Speicher steht.
Gut, dann haben wir Objektidentität.
Wir wissen jetzt, was identisch ist.
Hier gibt es eigentlich noch ein anderes Beispiel,
wo wir drei Kreise haben.
Das sind drei Objekte, die sind alle Objekte von Klasse Kreis.
Und diese zwei haben zufälligerweise genau die gleichen Attributwerte.
Das heißt, die haben einen anderen Namen in diesem Fall.
Also in der vorherigen Folie hatten die dann keinen Namen,
die waren anonym, aber das ist dann egal.
Hier haben wir einen anderen Namen, deshalb sehen wir sofort,
dass diese zwei gleich sind, aber nicht identisch.
Also man kann die auch deshalb nennen, und so ist es noch deutlicher,
dass es da um zwei unterschiedliche Objekte geht,
nicht um das gleiche Objekt.
Auch in UML kann man zum Beispiel mehrere Seiten haben,
wo man zwischen den Seiten zum Beispiel das Gleiche repräsentiert.
Genau, und das Wichtigste ist, wenn man das Ganze ausführt
und man hat dann die Objekte irgendwo im Speicher,
das heißt, das hier ist irgendwo im Speicher,
aber das hier ist irgendwo anders im Speicher.
Und das Gleiche für das, was da auf der letzten Folie stand.
Und in anderen Systemen, zum Beispiel in Objekt-Datenbank-Systemen,
hat man normalerweise noch eine extra Nummer,
also eine sogenannte OED, also Object ID,
die man dann auch da verwendet,
sodass man nicht immer das Ganze benennen muss,
sodass sich da die Namen zum Beispiel auch überlappen könnten.